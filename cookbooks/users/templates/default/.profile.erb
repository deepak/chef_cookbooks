# ~/.profile: executed by Bourne-compatible login shells.

if [ "$BASH" ]; then
  if [ -f ~/.bashrc ]; then
    . ~/.bashrc
  fi
fi

if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

<% if @set_rbenv_version %>
export RBENV_VERSION=1.9.3-p327
<% end %>
