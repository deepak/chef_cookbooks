# general
alias tree_all="tree -aRC -I .git"
alias du_report="du --max-depth=1 -h"
alias users_list="lastlog"
alias ll="ls -l"

# git
alias gk="gitk --all &"
alias gc="git status"
alias gd="git diff"
alias gp="git pull --rebase"
